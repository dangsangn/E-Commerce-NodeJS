# UPLOAD MODULE — Thiết kế chức năng Upload Image

> **Tài liệu thiết kế (design doc)** cho chức năng upload ảnh, dùng chung cho **Product** (ảnh sản phẩm) và **User Profile** (avatar) sau này.
>
> Tài liệu này viết theo phong cách _một Senior Engineer đang mentor_: không chỉ nói *làm gì*, mà giải thích *tại sao*, *khi nào dùng*, *khi nào KHÔNG dùng*, đánh đổi ra sao, và điều gì sẽ vỡ khi lên production.
>
> **Phạm vi:** Đây là bản thiết kế + code mẫu. Chưa phải là code đã merge. Mục tiêu là để bất kỳ ai trong team đọc xong đều hiểu và triển khai được đúng chuẩn dự án.

---

## Mục lục

1. [Overview — Tổng quan](#1-overview--tổng-quan)
2. [Mental Model — Mô hình tư duy](#2-mental-model--mô-hình-tư-duy)
3. [First Principles — Nguyên lý gốc](#3-first-principles--nguyên-lý-gốc)
4. [Internal Implementation — Kiến trúc bên trong](#4-internal-implementation--kiến-trúc-bên-trong)
5. [Step-by-Step Walkthrough — Đi qua từng bước](#5-step-by-step-walkthrough--đi-qua-từng-bước)
6. [Practical Examples — Ví dụ thực tế](#6-practical-examples--ví-dụ-thực-tế)
7. [Common Mistakes — Lỗi thường gặp](#7-common-mistakes--lỗi-thường-gặp)
8. [Trade-offs — Đánh đổi](#8-trade-offs--đánh-đổi)
9. [Performance Considerations — Hiệu năng](#9-performance-considerations--hiệu-năng)
10. [Security Considerations — Bảo mật](#10-security-considerations--bảo-mật)
11. [Real Production Experience — Kinh nghiệm production](#11-real-production-experience--kinh-nghiệm-production)
12. [Senior Engineer Notes — Ghi chú của Senior](#12-senior-engineer-notes--ghi-chú-của-senior)
13. [Interview Knowledge — Kiến thức phỏng vấn](#13-interview-knowledge--kiến-thức-phỏng-vấn)
14. [Best Practices — Thực hành tốt nhất](#14-best-practices--thực-hành-tốt-nhất)
15. [Related Concepts — Khái niệm liên quan](#15-related-concepts--khái-niệm-liên-quan)

---

## 1. Overview — Tổng quan

### Nó là gì?

**Upload Module** là một feature-module chịu trách nhiệm nhận ảnh từ client, kiểm tra tính hợp lệ, đẩy lên **Cloudinary** (dịch vụ lưu trữ + xử lý ảnh trên cloud), và trả về URL để các module khác (Product, User) lưu vào MongoDB.

Nó là một **service dùng chung** (shared/cross-cutting service). Product không tự upload; nó *gọi* Upload Module. User profile sau này cũng vậy. Đây là điểm mấu chốt của thiết kế — **một nơi duy nhất biết cách nói chuyện với Cloudinary**.

### Tại sao nó tồn tại?

Trong codebase hiện tại (`src/features/upload/services/index.ts`), bạn đã có `UploadService.uploadFromUrl()` — nhưng nó mới chỉ hỗ trợ upload từ URL, còn `uploadFromFile` / `uploadFromFiles` là hàm rỗng. Product hiện chỉ có route `POST /product/upload/link/:shopId` nhận **link ảnh có sẵn**, chứ chưa cho phép người dùng chọn file từ máy để upload.

Chức năng này giải quyết đúng khoảng trống đó: **cho phép upload file thật từ client**, đồng thời chuẩn hoá lại toàn bộ luồng để dùng lại được cho avatar user.

### Nó giải quyết vấn đề gì?

| Vấn đề | Cách module này giải quyết |
| --- | --- |
| Mỗi feature tự viết code Cloudinary → trùng lặp, khó bảo trì | Gom về 1 `UploadService` duy nhất |
| Không validate → user upload file `.exe` đổi tên thành `.jpg` | Validate MIME thật bằng "magic bytes" |
| File lớn làm sập RAM/bandwidth | Giới hạn size + số lượng qua Multer |
| Ảnh mồ côi (orphan) khi tạo sản phẩm thất bại | Chiến lược dọn dẹp (cleanup) + `public_id` |
| **Chưa có `product_id` khi cần upload ảnh** (vòng lặp gà–trứng) | Sinh `ObjectId` ở server *trước*, upload ảnh rồi mới tạo product — xem [mục 3, Vấn đề 4](#3-first-principles--nguyên-lý-gốc) |
| Ảnh nặng làm chậm trang web | Cloudinary transform: `q_auto,f_auto` |

---

## 2. Mental Model — Mô hình tư duy

Hãy hình dung Upload Module như **quầy lễ tân của một khách sạn ký gửi đồ (cloak room)**:

```
Khách (client) ──mang vali (file)──► Lễ tân (UploadService)
                                         │
                          1. Kiểm tra vali có phải bom không? (validate)
                          2. Cân xem có quá nặng không? (size limit)
                          3. Cất vào kho an toàn (Cloudinary)
                          4. Đưa lại tấm vé (secure_url + public_id)
                                         │
Khách ◄──── tấm vé (URL) ────────────────┘
```

Ba insight quan trọng từ mô hình này:

1. **File không "ở lại" trong server của bạn.** Server chỉ là người trung chuyển. Kho thật là Cloudinary. Server chỉ giữ lại **tấm vé** (`public_id` + `secure_url`) để sau này lấy lại hoặc huỷ.

2. **`public_id` là chìa khoá, `secure_url` là địa chỉ.**
   - `secure_url` = `https://res.cloudinary.com/<cloud>/image/upload/v123/products/abc.jpg` → để hiển thị.
   - `public_id` = `products/abc` → để **xoá** hoặc **biến đổi** ảnh. **Phải lưu cả hai vào DB**, nếu không bạn sẽ không bao giờ xoá được ảnh (đây là lỗi kinh điển ở mục 7).

3. **Biến đổi ảnh xảy ra lúc ĐỌC, không phải lúc GHI.** Bạn upload 1 ảnh gốc duy nhất. Khi cần thumbnail 200x200, bạn chỉ *chèn tham số vào URL* — Cloudinary sinh ra ảnh nhỏ đó on-the-fly và cache lại. Đây là tư duy trái ngược với "resize rồi lưu 5 phiên bản".

---

## 3. First Principles — Nguyên lý gốc

Trước khi nhìn code, hãy hiểu **bài toán kỹ thuật gốc**: *Làm sao đưa một chuỗi bytes từ trình duyệt người dùng lên một nơi lưu trữ, một cách an toàn và đáng tin cậy?*

### Vấn đề 1: HTTP không sinh ra để truyền file nhị phân

HTTP body mặc định là text. Để gửi file, trình duyệt dùng `multipart/form-data` — chia body thành nhiều "part", mỗi part có header riêng và ranh giới (boundary). Express (`express.json()`) **không** parse được định dạng này. Đó là lý do ta cần **Multer** — một middleware chuyên parse `multipart/form-data` thành `req.file` / `req.files`.

> Nếu không có Multer, `req.body` sẽ rỗng và bạn sẽ tưởng "client gửi thiếu file" trong khi thực ra server không biết đọc.

### Vấn đề 2: Lưu file ở đâu?

Ngày xưa (giới hạn trước đây), người ta lưu file vào **ổ đĩa server** (`/uploads`) rồi dùng `express.static` để serve. Điều này sinh ra một loạt vấn đề:

- **Không scale ngang được.** Có 3 server (pod) sau load balancer? Ảnh upload lên pod A không thấy được ở pod B.
- **Mất dữ liệu khi deploy.** Container restart = mất `/uploads` (trừ khi mount volume — thêm phức tạp).
- **Server phải gánh việc serve ảnh** — tốn bandwidth, tốn CPU.
- **Không có CDN** → user ở xa tải ảnh chậm.

Đây chính là lý do các dịch vụ **object storage** (S3, Cloudinary, GCS) ra đời: tách "lưu trữ file" khỏi "chạy application". Dự án của bạn đã chọn **Cloudinary** — và đó là lựa chọn tốt vì Cloudinary còn kèm luôn **image processing + CDN** trong một sản phẩm.

### Vấn đề 3: Ai chịu tải khi truyền bytes?

Có hai triết lý, và bạn đã chọn **cả hai** (thiết kế mở rộng):

```
(A) DIRECT UPLOAD QUA SERVER  ← MVP, làm trước
    Client ──file──► Server ──file──► Cloudinary
    Server "nhìn thấy" bytes → validate được tận nội dung.
    Nhược: tốn bandwidth server.

(B) SIGNED DIRECT-TO-CLOUDINARY  ← nâng cấp sau
    Client ──xin chữ ký──► Server (chỉ ký, không nhận file)
    Client ──file────────► Cloudinary (thẳng)
    Client ──xác nhận────► Server (lưu URL)
    Server KHÔNG thấy bytes → nhẹ, scale tốt, nhưng khó validate nội dung.
```

Nguyên lý thiết kế cốt lõi: **Controller và Service phải không quan tâm bytes đến từ đâu.** Nếu `UploadService` được thiết kế đúng, việc chuyển từ (A) sang (B) sau này chỉ là thêm một phương thức, không phải viết lại. Chúng ta sẽ thiết kế để đạt điều đó (xem mục 4).

### Vấn đề 4: Con gà hay quả trứng — tạo product trước, hay upload ảnh trước?

Đây là mâu thuẫn thiết kế then chốt của module này, và là lý do tài liệu được sửa lại.

Có hai ràng buộc **đối nghịch nhau**:

1. **Model bắt buộc phải có ảnh khi tạo product.** `product_thumb` (ảnh chính) là `required`. Không có URL ảnh → Mongoose reject → không tạo được product.
2. **Muốn xếp ảnh vào folder theo product** (`products/{shopId}/{productId}`) để dễ quản lý, dọn rác và phân quyền. Nhưng `productId` chỉ có *sau khi* đã tạo product.

→ Muốn upload trước để lấy URL thì **chưa có `productId`**; muốn tạo product trước để có `productId` thì **chưa có URL ảnh**. Đây đúng là bài toán con gà — quả trứng.

**Vì sao dự án chọn "upload ảnh trước"?** Vì **trải nghiệm người dùng (UX)**. Người dùng chọn ảnh và thấy ảnh tải lên / xem trước *ngay lập tức* trong khi vẫn đang điền form, thay vì điền hết form → bấm "Tạo" → *rồi mới* ngồi chờ upload (chậm; và nếu upload lỗi thì mất trắng dữ liệu form vừa nhập). Upload trước = phản hồi tức thì, không mất dữ liệu, và nếu người dùng bỏ ngang thì cũng **không** để lại bản ghi rác trong DB (chỉ còn ảnh trên cloud — xử lý bằng cron dọn rác, xem [mục 11](#11-real-production-experience--kinh-nghiệm-production)).

**Chìa khoá phá vòng lặp — một sự thật ít người để ý:**

> `_id` của MongoDB (`ObjectId`) **không cần được ghi vào DB mới tồn tại.** Nó chỉ là 12 byte sinh ở phía ứng dụng. Bạn hoàn toàn có thể `new mongoose.Types.ObjectId()` *trước cả* bước upload lẫn bước tạo document.

Nhờ đó luồng trở nên thẳng hàng, hết mâu thuẫn:

```
1) Server sinh sẵn productId = new ObjectId()        ← chưa đụng DB
2) Upload ảnh vào products/{shopId}/{productId}       ← đã có id để đặt tên folder
3) Tạo product với _id = productId + URL ảnh vừa có   ← thumb đã sẵn sàng
```

`productId` được **nhúng vào đường dẫn ảnh trên Cloudinary** ngay từ bước 2 — điều này không chỉ để gọn folder, mà còn là *bằng chứng sở hữu* dùng để kiểm tra ở bước 3 (xem [mục 5](#5-step-by-step-walkthrough--đi-qua-từng-bước) và [mục 10](#10-security-considerations--bảo-mật)).

> **Phân biệt HAI luồng, tuyệt đối đừng trộn lẫn:**
>
> - **Tạo mới** (product *chưa* tồn tại): dùng luồng "sinh id → upload → tạo" ở trên. Ownership được đảm bảo qua `shopId` lấy từ `req.user` (nhúng vào folder) + kiểm tra prefix `public_id` lúc tạo.
> - **Sửa / thêm ảnh** (product *đã* tồn tại): dùng `attachImages` gắn ảnh vào product có sẵn (mục 5.C). Đây mới là chỗ kiểm ownership qua DB (`product_shop === userId`).
>
> Bản doc trước đây gộp cả hai làm một (`attachImages` cho cả tạo mới) — chính là nguồn gốc mâu thuẫn. Đã tách bạch từ bản này.

---

## 4. Internal Implementation — Kiến trúc bên trong

### 4.1. Cấu trúc thư mục (theo đúng pattern feature-module của dự án)

Dự án đang theo mô hình mỗi feature là một module có `controller / service / routes / dto / model`. Upload là **cross-cutting service**, nên nó hơi khác: nó là *service dùng chung* + *middleware*, và không có model riêng (nó ghi vào model của Product/User).

```
src/features/upload/
├── services/
│   └── index.ts            # UploadService — bộ não, nói chuyện Cloudinary
├── middlewares/
│   └── multer.middleware.ts # Cấu hình Multer (memoryStorage + limits + fileFilter)
├── dto/
│   └── index.ts            # Zod schema: validate metadata (folder, resourceType...)
├── validators/
│   └── image.validator.ts  # Kiểm tra "magic bytes" — MIME thật của file
├── constants/
│   └── index.ts            # ALLOWED_MIME, MAX_FILE_SIZE, MAX_FILES, folder prefixes
├── controller/
│   └── index.ts            # (tuỳ chọn) route upload độc lập /api/v1/upload/*
└── UPLOAD_MODULE.md        # ← tài liệu này
```

> **Quyết định kiến trúc:** Upload có nên có *route riêng* (`POST /api/v1/upload/image`) hay *nhúng vào route của Product*? → Xem [mục 12](#12-senior-engineer-notes--ghi-chú-của-senior). Ngắn gọn: **nhúng vào route feature** để giữ RBAC + ownership, còn `UploadService` thì dùng chung. Với luồng upload-first, việc "nhúng" hiện ra thành hai route: `POST /product/upload/prepare` (tạo mới) và `PUT /product/upload/images/:productId` (sửa) — xem [mục 8.3](#83-route-riêng-vs-route-nhúng-feature).

### 4.2. Sơ đồ luồng dữ liệu (data flow) — Upload-first (2 request)

Vì đã chọn "upload ảnh trước" (Vấn đề 4), luồng tạo sản phẩm tách làm **hai request** rạch ròi. Request 1 chỉ upload ảnh (chưa đụng DB); Request 2 mới tạo product.

**Request 1 — `POST /product/upload/prepare` (multipart, CHƯA có product trong DB):**

```
┌─────────┐  multipart/form-data   ┌──────────────────────────────────────────────┐
│ Client  │ ─────────────────────► │               Express (app.ts)                 │
│(browser)│  field: "images"       │                                                │
└─────────┘                        │  1. loggingMiddleware (requestId)              │
     ▲                             │  2. apiKey (global)                            │
     │                             │  3. authentication (JWT → req.user)            │
     │                             │  4. protect('product').create (RBAC)           │
     │                             │  5. multer.array('images', 10) → buffers (RAM) │
     │                             │  6. validateImageBuffer() → magic bytes        │
     │                             │  7. Controller.prepareProductImages            │
     │ {productId, thumb, images}  │       → productId = new ObjectId()  (CHƯA ghi DB)│
     │ ◄───────────────────────────│       → folder = products/{shopId}/{productId} │
     │       OkResponse            │       → UploadService.uploadBuffer() (song song)│
     └─────────────────────────────│                       │                        │
                                    │                       ▼                        │
                                    │             cloudinary.upload_stream()          │
                                    └───────────────────────┬────────────────────────┘
                                                            │ HTTPS
                                                            ▼
                                               ┌───────────────────────────┐
                                               │         Cloudinary         │
                                               │  folder products/{shop}/{id}│
                                               │  → secure_url + public_id  │
                                               └───────────────────────────┘
   ⚠️  KHÔNG ghi MongoDB ở đây. Client giữ { productId, thumb, images } cho Request 2.
```

**Request 2 — `POST /product` (JSON, dùng lại `productId` + ảnh đã có):**

```
┌─────────┐  application/json                       ┌────────────────────────────────┐
│ Client  │ ─ { _id: productId, product_thumb,      │          Express (app.ts)         │
│         │     product_thumb_public_id, images } ─► │  auth + protect('product').create │
└─────────┘                                          │  Controller.createProduct         │
     ▲                                               │   → validate DTO (Zod)            │
     │                                               │   → validate prefix public_id     │
     │           { data: product }                   │      (products/{shop}/{productId})│
     │ ◄─────────────────────────────────────────────│   → tạo product _id = productId   │
     └───────────────────────────────────────────────┤     + inventory  (transaction)    │
                                                      └───────────────┬───────────────────┘
                                                                      ▼
                                                          ┌───────────────────────┐
                                                          │   MongoDB (Product)    │
                                                          │   _id: productId       │
                                                          │   product_thumb: url   │
                                                          │   product_images: [...] │
                                                          └───────────────────────┘
```

### 4.3. Lifecycle của một request upload (vòng đời)

```
[NHẬN] ──► [PHÂN GIẢI] ──► [XÁC THỰC] ──► [KIỂM TRA] ──► [ĐẨY LÊN] ──► [LƯU DB] ──► [DỌN DẸP nếu lỗi]
  │            │              │              │              │             │              │
 HTTP      Multer parse    JWT+RBAC       magic bytes    Cloudinary    Mongo update   rollback
 request   → req.file      → req.user     + size + MIME   upload_stream product_images  (xoá ảnh
                                                          → public_id                   nếu DB fail)
```

Điểm tinh tế nhất là **bước cuối cùng — DỌN DẸP**. Đây là nơi phần lớn hệ thống bị rò rỉ (xem mục 7 & 11).

> **Lưu ý với luồng upload-first:** Vòng đời trên áp dụng cho *một* request upload. Ở luồng tạo mới (Vấn đề 4), bước **[LƯU DB]** không nằm trong request upload (`prepare`) — request đó dừng ngay sau **[ĐẨY LÊN]** rồi trả `{ productId, images }` về client. Bản ghi product chỉ được ghi vào Mongo ở **request `POST /product` riêng biệt** sau đó. Vì vậy nếu người dùng bỏ ngang, ảnh trên Cloudinary trở thành *mồ côi* và được dọn bằng cron (mục 11), chứ không có bản ghi product rác nào.

### 4.4. Thiết kế `UploadService` — trái tim của module

Đây là code mẫu, viết đúng theo convention dự án (dùng `config`, `logger`, throw error class từ `core`):

```typescript
// src/features/upload/services/index.ts
import { v2 as cloudinary, UploadApiOptions, UploadApiResponse } from 'cloudinary'
import { Readable } from 'stream'
import config from '../../../configs'
import logger from '../../../loggers' // theo winston logger của dự án
import { InternalServerError } from '../../../core/error.response'

cloudinary.config({
  cloud_name: config.cloudinaryCloudName,
  api_key: config.cloudinaryApiKey,
  api_secret: config.cloudinaryApiSecret,
  secure: true, // luôn trả về https URL
})

export interface UploadResult {
  url: string        // secure_url — để hiển thị
  publicId: string   // public_id — để xoá / transform
  width: number
  height: number
  format: string
  bytes: number
}

export class UploadService {
  /**
   * (A) Direct upload — nhận buffer từ Multer memoryStorage, stream lên Cloudinary.
   * Đây là phương thức chính cho MVP.
   */
  static uploadBuffer = async (
    buffer: Buffer,
    options: UploadApiOptions = {},
  ): Promise<UploadResult> => {
    return new Promise((resolve, reject) => {
      const stream = cloudinary.uploader.upload_stream(
        {
          resource_type: 'image',
          // dùng transformation lúc upload để giới hạn kích thước gốc,
          // tránh lưu ảnh 8000x6000 vô nghĩa
          transformation: [{ width: 2000, height: 2000, crop: 'limit' }],
          ...options,
        },
        (error, result?: UploadApiResponse) => {
          if (error || !result) {
            logger.error('Cloudinary upload_stream failed', { error })
            return reject(new InternalServerError('Upload image failed'))
          }
          resolve(UploadService.toResult(result))
        },
      )
      Readable.from(buffer).pipe(stream)
    })
  }

  /**
   * Upload từ URL (đã có sẵn trong dự án) — refactor lại để trả về UploadResult chuẩn.
   */
  static uploadFromUrl = async (
    url: string,
    options: UploadApiOptions = {},
  ): Promise<UploadResult> => {
    try {
      const result = await cloudinary.uploader.upload(url, options)
      return UploadService.toResult(result)
    } catch (error) {
      logger.error('Cloudinary upload from URL failed', { error, url })
      throw new InternalServerError('Upload image from URL failed')
    }
  }

  /**
   * Xoá ảnh — CỰC KỲ QUAN TRỌNG cho cleanup và update.
   * Nhận public_id (không phải URL).
   */
  static destroy = async (publicId: string): Promise<void> => {
    try {
      await cloudinary.uploader.destroy(publicId)
    } catch (error) {
      // không throw để không làm hỏng luồng chính — chỉ log để job dọn rác xử lý sau
      logger.error('Cloudinary destroy failed', { error, publicId })
    }
  }

  /**
   * (B) Signed upload — cho tương lai. Server chỉ ký, không nhận file.
   * Client dùng chữ ký này để upload thẳng lên Cloudinary.
   */
  static createUploadSignature = (
    params: Record<string, string | number>,
    timestamp: number, // truyền từ ngoài vào; Date.now() không dùng trong service thuần
  ) => {
    const toSign = { timestamp, ...params }
    const signature = cloudinary.utils.api_sign_request(
      toSign,
      config.cloudinaryApiSecret,
    )
    return { signature, timestamp, apiKey: config.cloudinaryApiKey }
  }

  private static toResult = (r: UploadApiResponse): UploadResult => ({
    url: r.secure_url,
    publicId: r.public_id,
    width: r.width,
    height: r.height,
    format: r.format,
    bytes: r.bytes,
  })
}
```

**Vì sao thiết kế như vậy?**

- `uploadBuffer` là điểm mở rộng: controller chỉ đưa `buffer` + `folder`, không cần biết Cloudinary.
- `createUploadSignature` để sẵn cho phương án (B) — chuyển đổi tương lai chỉ là *thêm route*, không đụng vào `uploadBuffer`. Đây là **Open/Closed Principle** trong thực tế.
- `destroy` trả `void` và **nuốt lỗi** (chỉ log): xoá ảnh là thao tác "best-effort", không được phép làm sập request chính của user.
- `toResult` chuẩn hoá output → mọi caller đều nhận cùng một shape, dễ lưu DB.

### 4.5. Multer middleware — cổng vào

```typescript
// src/features/upload/middlewares/multer.middleware.ts
import multer from 'multer'
import { BadRequestError } from '../../../core/error.response'
import { ALLOWED_MIME, MAX_FILE_SIZE, MAX_FILES } from '../constants'

// memoryStorage: file nằm trong RAM dưới dạng Buffer, KHÔNG ghi ra đĩa.
// Phù hợp vì ta stream thẳng lên Cloudinary.
const storage = multer.memoryStorage()

const fileFilter: multer.Options['fileFilter'] = (_req, file, cb) => {
  // Lớp phòng thủ 1: chặn theo MIME khai báo (nhanh, nhưng có thể bị giả mạo)
  if (!ALLOWED_MIME.includes(file.mimetype)) {
    return cb(new BadRequestError(`Định dạng ${file.mimetype} không được hỗ trợ`))
  }
  cb(null, true)
}

export const uploadImage = multer({
  storage,
  fileFilter,
  limits: {
    fileSize: MAX_FILE_SIZE, // ví dụ 5 * 1024 * 1024 = 5MB
    files: MAX_FILES,        // ví dụ 10 ảnh/lần
  },
})

// Dùng: uploadImage.single('image')  |  uploadImage.array('images', 10)
```

### 4.6. Kiểm tra magic bytes — lớp phòng thủ thật sự

MIME type do client khai báo **không đáng tin** (kẻ tấn công đổi được). Ta phải đọc vài byte đầu file để biết định dạng thật:

```typescript
// src/features/upload/validators/image.validator.ts
import { BadRequestError } from '../../../core/error.response'

// Magic bytes (file signature) của các định dạng ảnh phổ biến
const SIGNATURES: { mime: string; bytes: number[] }[] = [
  { mime: 'image/jpeg', bytes: [0xff, 0xd8, 0xff] },
  { mime: 'image/png',  bytes: [0x89, 0x50, 0x4e, 0x47] },
  { mime: 'image/webp', bytes: [0x52, 0x49, 0x46, 0x46] }, // "RIFF"
  { mime: 'image/gif',  bytes: [0x47, 0x49, 0x46, 0x38] }, // "GIF8"
]

export const validateImageBuffer = (buffer: Buffer): string => {
  const matched = SIGNATURES.find((sig) =>
    sig.bytes.every((b, i) => buffer[i] === b),
  )
  if (!matched) {
    throw new BadRequestError('File không phải ảnh hợp lệ (magic bytes không khớp)')
  }
  return matched.mime
}
```

> Đây là điểm phân biệt code "chạy được" và code "production-grade". 90% tutorial bỏ qua bước này.

### 4.7. Thay đổi schema (Product & User)

**Product** — thêm gallery + lưu `public_id` để xoá được:

```typescript
// bổ sung vào src/features/product/model/index.ts
const imageSchema = new Schema(
  {
    url: { type: String, required: true },
    public_id: { type: String, required: true }, // ← BẮT BUỘC để xoá được
  },
  { _id: false },
)

// trong productSchema:
{
  product_thumb: { type: String, required: true },            // ảnh chính (đã có ở bước prepare)
  product_thumb_public_id: { type: String, required: true },  // ← BẮT BUỘC, đi cặp với thumb để xoá được
  product_images: { type: [imageSchema], default: [] },       // ← gallery mới
}
```

**User** — avatar (dùng lại y hệt UploadService):

```typescript
// bổ sung vào src/features/user/models/index.ts
{
  usr_avatar: { type: String, default: '' },            // đã có sẵn
  usr_avatar_public_id: { type: String, default: '' },  // ← thêm mới để xoá avatar cũ
}
```

> **Bài học chung:** bất cứ chỗ nào lưu `url`, phải lưu kèm `public_id`. Coi chúng như một cặp không thể tách rời.

**DTO tạo product** (`src/features/product/dto/create.dto.ts`) phải nhận thêm dữ liệu trả về từ bước `prepare`:

```typescript
export const createProductSchema = z.object({
  _id: z.string(),                          // ← productId đã sinh ở bước prepare
  product_name: z.string(),
  product_thumb: z.string(),                // ← bỏ .nullable(): upload-first nên luôn có thumb
  product_thumb_public_id: z.string(),      // ← đi cặp với thumb
  product_images: z
    .array(z.object({ url: z.string(), public_id: z.string() }))
    .default([]),                           // ← gallery client gửi lại
  product_price: z.number(),
  product_quantity: z.number(),
  product_type: z.string(),
  product_attributes: z.any(),
})
```

> Bản DTO cũ để `product_thumb: z.string().nullable()` và **thiếu** `product_thumb_public_id` — trong khi model lại bắt buộc field này → tạo product luôn fail validation ở Mongoose. Đây là một biểu hiện khác của cùng mâu thuẫn ở Vấn đề 4, được sửa luôn ở đây.

---

## 5. Step-by-Step Walkthrough — Đi qua từng bước

Ta đi qua trọn vẹn luồng **tạo sản phẩm mới có ảnh**, theo đúng thứ tự "upload trước" (Vấn đề 4). Luồng chính gồm **hai request** (5.A → 5.B); mục 5.C mô tả luồng *thêm ảnh cho sản phẩm đã tồn tại*.

### 5.A — Request 1: Upload ảnh trước (`POST /product/upload/prepare`)

Mục tiêu: nhận file → sinh `productId` → đẩy ảnh lên Cloudinary → trả về `productId` + danh sách ảnh. **Chưa tạo product trong DB.**

#### Input

```
POST /api/v1/product/upload/prepare
Headers:
  x-api-key: <key>
  x-client-id: <userId>
  authorization: <accessToken>
  Content-Type: multipart/form-data
Body (form-data):
  images: <file1.jpg>, <file2.png>
```

#### Processing — từng stage một

**Stage 1 — Middleware toàn cục (app.ts):**
`loggingMiddleware` gắn `requestId` → `apiKey` kiểm tra header `x-api-key` → nếu thiếu, `ForbiddenError('Key is empty')`.

**Stage 2 — Authentication (checkAuth.ts):**
Đọc `x-client-id` + `authorization`, verify JWT. Gắn `req.user = { userId, email, roles }`. Sai token → `UnauthorizedError`.

**Stage 3 — RBAC (rbac.ts):**
`protect('product').create` — kiểm tra user có quyền `product.create`.

> Vì sao ở đây dùng `create` chứ **không** phải `update`? Vì ta đang ở luồng *tạo mới* — product chưa tồn tại, không có gì trong DB để "sửa". `shopId = req.user.userId` chính là căn cứ ownership (được nhúng vào folder ảnh). Ngược lại, luồng *thêm ảnh cho product có sẵn* (5.C) mới dùng `update` + kiểm ownership qua DB.

**Stage 4 — Multer (`uploadImage.array('images', 10)`):**
Parse `multipart/form-data`. Với mỗi file: chạy `fileFilter` (chặn MIME lạ), kiểm tra `fileSize`. Kết quả → `req.files: Express.Multer.File[]`, mỗi file có `.buffer`. Nếu vượt limit → Multer throw `MulterError` (map sang `BadRequestError`, xem mục 7).

**Stage 5 — Controller:**

```typescript
// src/features/product/controller/index.ts
prepareProductImages = async (req: Request, res: Response) => {
  const userId = req.user?.userId
  const files = req.files as Express.Multer.File[]
  if (!files?.length) throw new BadRequestError('Không có file nào được gửi lên')

  // validate magic bytes TRƯỚC khi tốn công đẩy lên Cloudinary
  files.forEach((f) => validateImageBuffer(f.buffer))

  const data = await ProductServiceFactory.prepareImages({
    shopId: userId as string,
    files,
  })
  return OkResponse.send(res, { message: 'Upload ảnh thành công', data })
}
```

**Stage 6 — Service (sinh id + upload + rollback):**

```typescript
// trong ProductServiceFactory
static prepareImages = async ({
  shopId,
  files,
}: {
  shopId: string
  files: Express.Multer.File[]
}) => {
  // 1) Sinh productId TRƯỚC — ObjectId không cần ghi DB mới tồn tại (Vấn đề 4)
  const productId = new mongoose.Types.ObjectId()
  const folder = `products/${shopId}/${productId}`

  // 2) Upload song song. Dùng allSettled để lỗi giữa chừng vẫn dọn được ảnh đã lên
  const settled = await Promise.allSettled(
    files.map((f) => UploadService.uploadBuffer(f.buffer, { folder })),
  )
  const ok = settled.filter((s) => s.status === 'fulfilled').map((s) => s.value)
  const failed = settled.filter((s) => s.status === 'rejected')
  if (failed.length) {
    await Promise.all(ok.map((r) => UploadService.destroy(r.publicId))) // rollback
    throw new InternalServerError(`Upload thất bại ${failed.length}/${files.length} ảnh`)
  }

  const images = ok.map((r) => ({ url: r.url, public_id: r.publicId }))

  // 3) Trả id + ảnh cho client giữ, gửi lại ở bước tạo product
  return {
    productId: productId.toString(),
    thumb: images[0], // mặc định ảnh đầu làm thumb; client có thể đổi
    images,
  }
}
```

#### Output

```json
{
  "message": "Upload ảnh thành công",
  "statusCode": 200,
  "data": {
    "productId": "665f0c2a9b1e4a0012a3b4c5",
    "thumb": { "url": "https://res.cloudinary.com/demo/image/upload/v1/products/<shop>/665f.../a.jpg", "public_id": "products/<shop>/665f.../a" },
    "images": [
      { "url": "https://res.cloudinary.com/demo/image/upload/v1/products/<shop>/665f.../a.jpg", "public_id": "products/<shop>/665f.../a" },
      { "url": "https://res.cloudinary.com/demo/image/upload/v1/products/<shop>/665f.../b.png", "public_id": "products/<shop>/665f.../b" }
    ]
  }
}
```

Client giữ nguyên khối `data` này (đặc biệt là `productId` và `public_id` của từng ảnh) để gửi lại ở Request 2.

### 5.B — Request 2: Tạo product (`POST /product`)

#### Input

```
POST /api/v1/product          (Content-Type: application/json)
Body:
  {
    "_id": "665f0c2a9b1e4a0012a3b4c5",          // ← productId từ bước prepare
    "product_name": "Áo thun basic",
    "product_thumb": "https://.../665f.../a.jpg",
    "product_thumb_public_id": "products/<shop>/665f.../a",
    "product_images": [ { "url": "...", "public_id": "products/<shop>/665f.../a" }, ... ],
    "product_price": 199000,
    "product_quantity": 50,
    "product_type": "CLOTHING",
    "product_attributes": { "brand": "H&M", "size": "M", "color": "đen" }
  }
```

#### Processing

Controller `createProduct` validate DTO (đã cập nhật ở [mục 4.7](#47-thay-đổi-schema-product--user)) rồi gọi service. Điểm mấu chốt **bảo mật** nằm ở service — kiểm tra ảnh có thật sự thuộc về shop này không:

```typescript
// trong ProductServiceFactory
static createProduct = (payload: any) => {
  const ProductClass = this.productRegister[payload.product_type]
  if (!ProductClass) throw new BadRequestError('Invalid product type')

  // ✅ Chống "nhận vơ" ảnh: mọi public_id PHẢI nằm trong folder của chính shop + product này.
  //    productId do server sinh (bước prepare), shopId lấy từ req.user — client không kiểm soát
  //    được cả hai, nên không thể mạo nhận ảnh của shop khác hay bịa đường dẫn.
  const prefix = `products/${payload.product_shop}/${payload._id}`
  const publicIds = [
    payload.product_thumb_public_id,
    ...(payload.product_images ?? []).map((img: any) => img.public_id),
  ]
  if (publicIds.some((pid) => !pid?.startsWith(prefix))) {
    throw new ForbiddenError('Ảnh không thuộc quyền sở hữu của bạn')
  }

  // Tạo product với CHÍNH _id đã sinh ở bước prepare (không để Mongo tự sinh id mới)
  return new ProductClass(payload).createProduct()
}
```

> **Thay đổi trong `ProductService.createProduct` (transaction):** bản cũ lấy product `_id` **trùng** với `_id` của sub-doc clothing/electronic. Nay truyền thẳng `payload._id` (id từ bước prepare) làm product `_id`, còn sub-doc giữ `_id` riêng. Nhờ vậy folder ảnh `products/{shop}/{productId}` khớp đúng `_id` của product. Việc tạo `inventory` trong cùng transaction giữ nguyên.

#### Output

```json
{
  "message": "Tạo sản phẩm thành công",
  "statusCode": 200,
  "data": {
    "_id": "665f0c2a9b1e4a0012a3b4c5",
    "product_name": "Áo thun basic",
    "product_thumb": "https://.../665f.../a.jpg",
    "product_images": [ { "url": "...", "public_id": "products/<shop>/665f.../a" } ]
  }
}
```

### 5.C — Luồng SỬA: thêm ảnh cho product đã tồn tại (`PUT /product/upload/images/:productId`)

Đây là luồng **khác** với tạo mới: product **đã có** trong DB, ta chỉ bổ sung ảnh vào gallery. Vì product tồn tại nên **ownership kiểm qua DB** và RBAC dùng `update`:

```typescript
static attachImages = async ({
  productId,
  files,
  shopId,
}: {
  productId: string
  files: Express.Multer.File[]
  shopId: string
}) => {
  // Ownership qua DB: đúng product của shop này?
  const product = await ProductRepository.getProductByIdOwner(productId, shopId)
  if (!product) throw new BadRequestError('Product not found')

  files.forEach((f) => validateImageBuffer(f.buffer))

  const folder = `products/${shopId}/${productId}`
  const results = await Promise.all(
    files.map((f) => UploadService.uploadBuffer(f.buffer, { folder })),
  )
  const newImages = results.map((r) => ({ url: r.url, public_id: r.publicId }))

  try {
    product.product_images.push(...newImages)
    await product.save()
    return product.product_images
  } catch (dbErr) {
    await Promise.all(newImages.map((img) => UploadService.destroy(img.public_id))) // rollback
    throw dbErr
  }
}
```

> So với bản cũ, luồng 5.C **bỏ** nhánh `if (!product.product_thumb)` gán thumb — vì model đã bắt buộc `product_thumb` ngay từ lúc tạo (5.B), một product tồn tại thì chắc chắn đã có thumb. Giữ nhánh đó là dấu hiệu còn sót lại của tư duy "tạo product rỗng rồi mới có ảnh" — chính là mâu thuẫn ở Vấn đề 4.

### Hiển thị ở frontend — sức mạnh của transform

Không cần lưu nhiều phiên bản. Frontend tự chèn tham số vào URL:

```
Ảnh gốc:      .../upload/v1/products/.../a.jpg
Thumbnail:    .../upload/w_200,h_200,c_fill,q_auto,f_auto/v1/products/.../a.jpg
Ảnh sản phẩm: .../upload/w_800,q_auto,f_auto/v1/products/.../a.jpg
```

- `w_`, `h_` = kích thước; `c_fill` = crop cho vừa; `q_auto` = chất lượng tự động; `f_auto` = định dạng tự động (trả WebP/AVIF cho trình duyệt hỗ trợ).

---

## 6. Practical Examples — Ví dụ thực tế

### 6.1. Beginner — Upload 1 avatar cho user (dùng lại UploadService)

Đây chính là lý do ta gom vào service dùng chung. Toàn bộ code cho avatar chỉ như sau:

```typescript
// src/features/user/routes/index.ts
router.use(authentication)
router.patch(
  '/me/avatar',
  uploadImage.single('avatar'),
  asyncHandler(UserController.updateAvatar),
)

// src/features/user/controllers/index.ts
updateAvatar = async (req: Request, res: Response) => {
  const userId = req.user?.userId
  if (!req.file) throw new BadRequestError('Thiếu file avatar')

  validateImageBuffer(req.file.buffer)

  const user = await UserService.findById(userId)
  const oldPublicId = user.usr_avatar_public_id

  const result = await UploadService.uploadBuffer(req.file.buffer, {
    folder: `users/${userId}/avatar`,
    transformation: [{ width: 400, height: 400, crop: 'fill', gravity: 'face' }],
  })

  user.usr_avatar = result.url
  user.usr_avatar_public_id = result.publicId
  await user.save()

  // xoá avatar cũ SAU khi lưu thành công (best-effort)
  if (oldPublicId) await UploadService.destroy(oldPublicId)

  return OkResponse.send(res, { message: 'Cập nhật avatar thành công', data: { avatar: result.url } })
}
```

> Chú ý `gravity: 'face'` — Cloudinary tự phát hiện khuôn mặt để crop avatar cho đẹp. Đây là ví dụ "biến đổi lúc upload" hợp lý cho avatar (vì avatar luôn vuông).

### 6.2. Intermediate — Upload nhiều ảnh + tự động rollback

Xem `prepareImages` (mục 5.A) cho luồng tạo mới và `attachImages` (mục 5.C) cho luồng sửa. Điểm intermediate: **upload song song** để nhanh, nhưng phải **rollback ảnh** khi có lỗi — dù là lỗi upload giữa chừng hay lỗi lưu DB.

Có hai kiểu rollback cần phân biệt:

1. **Lỗi upload giữa chừng** (dùng cho `prepareImages`): `Promise.all` sẽ reject ngay khi *một* ảnh lỗi, nhưng các ảnh kia có thể đã lên Cloudinary → mồ côi. Dùng `Promise.allSettled` để gom cả thành công/thất bại rồi tự dọn phần đã lên:

```typescript
const settled = await Promise.allSettled(
  files.map((f) => UploadService.uploadBuffer(f.buffer, { folder })),
)
const ok = settled.filter((s) => s.status === 'fulfilled').map((s) => s.value)
const failed = settled.filter((s) => s.status === 'rejected')

if (failed.length) {
  // có ảnh lỗi → dọn hết ảnh đã lên rồi báo lỗi (all-or-nothing)
  await Promise.all(ok.map((r) => UploadService.destroy(r.publicId)))
  throw new InternalServerError(`Upload thất bại ${failed.length}/${files.length} ảnh`)
}
```

### 6.3. Production — Signed direct upload (phương án B)

Khi traffic lớn, bạn không muốn bytes đi qua server. Luồng:

```typescript
// 1) Server cấp chữ ký (KHÔNG nhận file)
// src/features/upload/controller/index.ts
getUploadSignature = async (req: Request, res: Response) => {
  const userId = req.user?.userId
  const timestamp = Math.floor(nowMs() / 1000) // nowMs() lấy từ tầng ngoài
  const folder = `products/${userId}`
  const { signature, apiKey } = UploadService.createUploadSignature({ folder }, timestamp)
  return OkResponse.send(res, {
    data: { signature, timestamp, apiKey, folder, cloudName: config.cloudinaryCloudName },
  })
}
```

```javascript
// 2) Client upload THẲNG lên Cloudinary
const form = new FormData()
form.append('file', file)
form.append('api_key', apiKey)
form.append('timestamp', timestamp)
form.append('signature', signature)
form.append('folder', folder)
const res = await fetch(`https://api.cloudinary.com/v1_1/${cloudName}/image/upload`, {
  method: 'POST', body: form,
})
const { secure_url, public_id } = await res.json()

// 3) Client gọi lại server để LƯU URL (server verify public_id thuộc folder của user)
await api.post(`/product/${productId}/images/confirm`, { url: secure_url, public_id })
```

> **Rủi ro của (B):** server không thấy bytes → không validate magic bytes được. Bù lại bằng: (a) giới hạn `folder`/`resource_type` trong chữ ký, (b) bật **Cloudinary moderation** (`moderation: 'aws_rek'`), (c) ở bước confirm, gọi Cloudinary Admin API để xác minh `public_id` thật sự tồn tại và có `resource_type = image`, đúng folder của user.

---

## 7. Common Mistakes — Lỗi thường gặp

| # | Lỗi | Vì sao xảy ra | Cách tránh |
| --- | --- | --- | --- |
| 1 | **Không lưu `public_id`, chỉ lưu URL** | Tutorial chỉ dạy hiển thị ảnh | Luôn lưu cặp `{url, public_id}`. Không có `public_id` = không xoá được ảnh cũ → phình storage vĩnh viễn |
| 2 | **Tin vào `file.mimetype`** | Multer báo MIME theo header client gửi | Kiểm tra **magic bytes** (mục 4.6). Header giả mạo được |
| 3 | **Dùng `multer.diskStorage()` rồi quên xoá** | Copy tutorial | Dùng `memoryStorage()` + stream thẳng. Không đụng đĩa server |
| 4 | **Không xử lý `MulterError`** | Multer throw lỗi kiểu riêng, không phải error class của dự án | Viết middleware map `MulterError` → `BadRequestError` (xem dưới) |
| 5 | **Ảnh mồ côi (orphan) khi DB fail** | Upload xong mới lưu DB, DB lỗi → ảnh vẫn nằm trên cloud | `try/catch` quanh `save()`, fail thì `destroy()` (mục 5) |
| 6 | **Upload đồng bộ tuần tự (for-await)** | Không biết `Promise.all` | Upload song song. 10 ảnh tuần tự = 10x thời gian |
| 7 | **Resize/lưu nhiều phiên bản trong DB** | Tư duy "lưu sẵn thumbnail" | Dùng Cloudinary transform URL — 1 ảnh gốc, mọi kích thước sinh on-the-fly |
| 8 | **Không giới hạn `files` count** | Chỉ set `fileSize` | Set cả `limits.files`. Không thì 10.000 ảnh nhỏ vẫn làm sập |
| 9 | **Đổi avatar không xoá avatar cũ** | Chỉ ghi đè URL trong DB | Lưu `public_id` cũ → `destroy()` sau khi lưu URL mới |
| 10 | **Để lộ `api_secret` ra client** | Copy config lên frontend | `api_secret` chỉ ở server. Client dùng signature, không dùng secret |

**Middleware xử lý MulterError (lỗi #4) — nên có:**

```typescript
// src/features/upload/middlewares/multer.middleware.ts (bổ sung)
import { MulterError } from 'multer'
import { BadRequestError } from '../../../core/error.response'
import { Request, Response, NextFunction } from 'express'

export const handleMulterError = (
  err: any, _req: Request, _res: Response, next: NextFunction,
) => {
  if (err instanceof MulterError) {
    if (err.code === 'LIMIT_FILE_SIZE') return next(new BadRequestError('Ảnh vượt quá dung lượng cho phép'))
    if (err.code === 'LIMIT_FILE_COUNT') return next(new BadRequestError('Vượt quá số lượng ảnh cho phép'))
    return next(new BadRequestError(`Lỗi upload: ${err.message}`))
  }
  next(err)
}
```

---

## 8. Trade-offs — Đánh đổi

### 8.1. Direct upload (A) vs Signed upload (B)

| Tiêu chí | (A) Direct qua server | (B) Signed direct-to-Cloudinary |
| --- | --- | --- |
| Độ phức tạp | Thấp — 1 endpoint | Cao — ký + confirm + verify |
| Bandwidth server | Tốn (bytes đi qua) | Gần như 0 |
| Validate nội dung (magic bytes) | Dễ, đầy đủ | Khó — server không thấy bytes |
| Scale với ảnh lớn / traffic cao | Kém hơn | Rất tốt |
| Kiểm soát / rollback | Chặt | Lỏng hơn (cần webhook/confirm) |
| Phù hợp | MVP, admin upload ảnh sản phẩm | Upload số lượng lớn từ end-user |

**Khuyến nghị:** Bắt đầu (A) cho cả product và avatar. Chỉ chuyển (B) khi đo được server bị nghẽn bandwidth vì upload. Đừng tối ưu sớm.

### 8.2. Cloudinary transform (lúc đọc) vs Sharp pre-process (lúc ghi)

| Advantage (Cloudinary transform) | Disadvantage |
| --- | --- |
| Không tốn CPU server | Phụ thuộc vendor (lock-in) |
| 1 ảnh gốc → vô hạn kích thước | Chi phí transform tính theo credit |
| Có CDN + `f_auto` (WebP/AVIF) | URL lộ khả năng transform (cần giới hạn nếu lo abuse) |
| Không cần deploy lại khi đổi kích thước | Cần internet tới Cloudinary khi render lần đầu |

### 8.3. Route riêng vs Route nhúng feature

| Route nhúng trong feature product (khuyến nghị) | Route riêng `POST /upload/image` |
| --- | --- |
| Giữ được ownership + RBAC theo resource | Mất ngữ cảnh resource |
| URL gắn với vòng đời product | Tách rời, dễ mồ côi |
| Rõ ràng ai được upload gì | Phải tự kiểm tra quyền thủ công |

Với luồng upload-first, "nhúng trong feature product" được hiện thực bằng **hai endpoint** phân vai rõ ràng:

| Endpoint | Khi nào | RBAC | Kiểm ownership |
| --- | --- | --- | --- |
| `POST /product/upload/prepare` | **Tạo mới** — product chưa tồn tại | `create` | Qua `shopId` (từ `req.user`) nhúng vào folder |
| `PUT /product/upload/images/:productId` | **Sửa** — thêm ảnh cho product có sẵn | `update` | Qua DB (`product_shop === userId`) |

> Đừng gộp hai endpoint này làm một. Chúng khác nhau ở cả *ngữ nghĩa quyền* (`create` vs `update`) lẫn *cách xác thực ownership* (folder-prefix vs DB). Gộp lại chính là nguồn gốc mâu thuẫn ở Vấn đề 4.

---

## 9. Performance Considerations — Hiệu năng

- **Memory (RAM):** `memoryStorage()` giữ toàn bộ file trong RAM. Với `MAX_FILE_SIZE = 5MB` × `MAX_FILES = 10` × N request đồng thời → RAM có thể tăng nhanh. Công thức ước lượng peak: `5MB × 10 × concurrency`. Đây là lý do **phải** đặt `limits`. Nếu cần ảnh rất lớn, cân nhắc `diskStorage` tạm hoặc chuyển sang (B).
- **CPU:** Vì không resize bằng Sharp, CPU server gần như không bị ảnh hưởng — công việc nặng đẩy hết sang Cloudinary. Đây là lợi ích lớn của kiến trúc này.
- **Network:** (A) tốn bandwidth gấp đôi (client→server, server→Cloudinary). Đo bằng metric egress. Khi con số này lớn → tín hiệu chuyển (B).
- **Latency:** Upload song song `Promise.all` giảm thời gian từ `tuần tự = Σ` xuống `song song ≈ max`. Nhưng đừng song song vô hạn — với hàng chục ảnh nên dùng **concurrency pool** (ví dụ `p-limit`, 3–5 luồng) để không nghẽn socket.
- **Scalability:** Vì file không nằm trên đĩa server, module này **stateless** → scale ngang thoải mái (nhiều pod sau load balancer). Đây là điểm mạnh so với `/uploads` local.

---

## 10. Security Considerations — Bảo mật

Upload file là **một trong những bề mặt tấn công nguy hiểm nhất** của web app. Đây là các vector và cách phòng:

| Attack vector | Mô tả | Phòng thủ |
| --- | --- | --- |
| **Malicious file / RCE** | Upload file thực thi (`.php`, `.exe`) giả làm ảnh | `resource_type: 'image'` + magic bytes + Cloudinary không thực thi file |
| **MIME spoofing** | Đổi đuôi `.jpg` cho file độc | Magic bytes (mục 4.6), không tin `file.mimetype` |
| **DoS qua file lớn** | Upload file 2GB làm cạn RAM/disk | `limits.fileSize` + `limits.files` |
| **Zip bomb / decompression bomb** | Ảnh nhỏ giải nén thành khổng lồ | `transformation width/height limit` khi upload; Cloudinary tự giới hạn |
| **SVG XSS** | SVG chứa `<script>` → XSS khi render inline | **Không cho phép `image/svg+xml`** trong `ALLOWED_MIME` |
| **Nội dung không phù hợp** | Ảnh khiêu dâm/bạo lực | Cloudinary moderation (`aws_rek` / `webpurify`) cho (B) |
| **IDOR / ghi đè ảnh người khác** | User A sửa ảnh sản phẩm của B | Kiểm tra **ownership** (`product_shop === userId`) trước khi upload (luồng sửa 5.C) |
| **Mạo nhận ảnh khi tạo (upload-first)** | Ở bước `POST /product`, client gửi `public_id`/`url` trỏ tới ảnh của shop khác, hoặc bịa đường dẫn | Validate **prefix**: mọi `public_id` phải bắt đầu bằng `products/{shopId}/{productId}`, với `shopId` lấy từ `req.user` và `productId = body._id` (mục 5.B). Client không kiểm soát được cả hai |
| **Lộ `api_secret`** | Secret rơi vào frontend | Chỉ ở server (`config`, `.env`), không commit, không trả ra API |
| **SSRF qua `uploadFromUrl`** | Truyền URL nội bộ (`http://169.254.169.254`) để dò metadata | Whitelist domain / chặn IP nội bộ khi dùng upload-from-URL |

> **SSRF là bẫy tinh vi.** Route `POST /product/upload/link/:shopId` hiện tại nhận URL tuỳ ý và đưa cho Cloudinary tải. Cloudinary tải hộ nên rủi ro SSRF phía server bạn thấp, nhưng vẫn nên **validate URL** (chỉ `http/https`, chặn IP private) để tránh lạm dụng.

---

## 11. Real Production Experience — Kinh nghiệm production

**Điều gì hoạt động tốt trong production:**

- **Object storage + CDN + transform-on-read** là mô hình chuẩn công nghiệp (Cloudinary, imgix, Thumbor). Nó loại bỏ gần như toàn bộ vấn đề vận hành của việc lưu file trên server.
- **Lưu `public_id`** trả cổ tức về sau: mọi tính năng "xoá ảnh cũ", "dọn rác", "audit" đều dựa vào nó.
- **Idempotency:** production nên đặt `public_id` xác định (ví dụ `products/<id>/<hash-nội-dung>`) để upload lại cùng ảnh không tạo bản trùng.

**Điều gì vỡ trong production:**

- **Orphan images (ảnh mồ côi)** là vấn đề #1, và luồng **upload-first càng làm nó nổi bật**: user gọi `prepare` (ảnh đã lên cloud) → chưa gọi `POST /product` → bỏ đi. Ảnh nằm mãi trên cloud, tốn tiền. Có hai chiến lược dọn, chọn theo cách đặt folder:
  - **(Khuyến nghị cho thiết kế này) Upload thẳng vào `products/{shopId}/{productId}` + cron đối chiếu.** Vì `productId` chỉ được ghi vào Mongo khi product thật sự được tạo, một **cron job** có thể liệt kê các folder `products/*/*` trên Cloudinary, đối chiếu với `_id` trong DB, và xoá folder nào **không có product tương ứng** và cũ hơn N giờ. Không cần bước `rename`.
  - **(Thay thế) Upload vào `tmp/` rồi `rename`.** Upload vào `tmp/{shopId}/...`, cron dọn `tmp/` cũ > 24h; khi tạo product thật thì `rename` sang folder chính. Hợp khi bạn *không* muốn sinh id trước — nhưng ở đây ta đã sinh id trước nên cách trên gọn hơn.
- **Timeout với ảnh lớn / mạng chậm.** Client mạng yếu upload ảnh 5MB → request treo. Cần đặt timeout hợp lý + cho client retry; hoặc chuyển (B) để bỏ qua server.
- **Chi phí bùng nổ vì transform.** Mỗi biến thể URL mới = 1 lần transform tính phí. Nếu frontend sinh URL tuỳ tiện (mỗi lần một `w_` khác) → nổ credit. Giải pháp: **định nghĩa sẵn vài preset** (`thumb`, `card`, `detail`) và chỉ dùng chúng.
- **Region latency:** ảnh gốc ở region xa user → lần transform đầu chậm. CDN của Cloudinary xử lý phần lớn, nhưng cần bật đúng.

**Vấn đề scale thường gặp:**

- Bandwidth server ở phương án (A) là trần cứng. Khi upload trở thành 20–30% traffic, chuyển (B).
- Rate limit của Cloudinary — production cần theo dõi và có hàng đợi (queue) nếu upload hàng loạt (ví dụ import 10.000 sản phẩm). Dự án đã có **RabbitMQ/Kafka** → đây là nơi lý tưởng để đẩy job upload nền.

---

## 12. Senior Engineer Notes — Ghi chú của Senior

- **Tách "storage provider" khỏi "business logic".** `UploadService` hiện gắn cứng Cloudinary. Nếu muốn phòng vendor lock-in, định nghĩa một interface `IStorageProvider { uploadBuffer, destroy, sign }` và cho `CloudinaryProvider implements IStorageProvider`. Sau này đổi sang S3 chỉ là viết provider mới. **Nhưng đừng làm sớm** — YAGNI. Ghi chú lại ranh giới này là đủ.

- **Vị trí đặt endpoint = quyết định về quyền sở hữu (ownership).** Nhúng upload vào feature product thay vì một `/upload/image` "trần" — vì nó *ép* bạn gắn RBAC + ownership vào đúng resource. Endpoint upload trần là nơi lỗ hổng IDOR sinh ra.

- **Upload-first làm ownership khó hơn một bậc — và đây là mấu chốt dễ sai.** Ở luồng tạo mới, product *chưa có trong DB* nên bạn **không thể** kiểm `product_shop === userId` như thường lệ (chưa có `product_shop`). Thay vào đó, ownership được đảm bảo bằng hai lớp: (1) `shopId` lấy từ `req.user` — *không* từ input client — rồi **nhúng vào đường dẫn folder** `products/{shopId}/{productId}` lúc upload; (2) lúc `POST /product`, **validate prefix** mọi `public_id` phải khớp `products/{shopId}/{productId}`. Nhờ (1)+(2), một `public_id` hợp lệ *chính là* bằng chứng "ảnh này do đúng shop đó upload cho đúng product đó". Nếu quên (2), client có thể gửi lên `public_id` của shop khác → rò rỉ/nhận vơ ảnh.

- **`public_id` là nợ kỹ thuật nếu quên từ đầu.** Thêm cột `public_id` sau khi đã có hàng nghìn ảnh chỉ lưu URL là một cuộc di trú (migration) đau đớn — bạn phải parse `public_id` ngược từ URL. Làm đúng từ ngày 1.

- **Cleanup là bài toán phân tán, không phải bài toán code.** Dù `try/catch` tốt đến đâu, vẫn sẽ có ảnh mồ côi (process bị kill giữa chừng, mạng đứt). Chấp nhận điều đó và xây **cron dọn rác** như một tuyến phòng thủ thứ hai. Đừng cố đạt "0 mồ côi" bằng code đồng bộ — bất khả thi.

- **Đừng validate ảnh bằng `express.json()`-style DTO.** File không phải JSON. Zod dùng để validate **metadata đi kèm** (folder, alt text, thứ tự ảnh), không phải bytes. Bytes validate bằng magic bytes + Multer limits.

- **Log có `requestId`.** Dự án đã có `loggingMiddleware` gắn `requestId`. Mọi log trong `UploadService` nên đi qua `logger` để trace được "ảnh nào, request nào, user nào". Khi debug ảnh mồ côi lúc 2h sáng, bạn sẽ cảm ơn điều này.

---

## 13. Interview Knowledge — Kiến thức phỏng vấn

**Câu hỏi thường gặp:**

1. *"Tại sao không lưu ảnh vào server mà dùng object storage?"*
   → Stateless server, scale ngang, tách storage khỏi compute, có CDN, không mất dữ liệu khi deploy.

2. *"`multipart/form-data` khác gì `application/json`?"*
   → multipart chia body thành nhiều part có boundary, truyền được nhị phân; JSON là text thuần, không truyền file hiệu quả.

3. *"Làm sao chống upload file độc?"*
   → Magic bytes (không tin MIME header), `resource_type: image`, giới hạn size/count, chặn SVG, moderation, storage không thực thi file.

4. *"`memoryStorage` vs `diskStorage`?"*
   → memory: nhanh, stream thẳng lên cloud, tốn RAM; disk: chịu file lớn, nhưng phải dọn và không scale tốt.

5. *"Signed upload hoạt động thế nào và vì sao an toàn?"*
   → Server ký `sha1(params + api_secret)`; client gửi kèm signature; Cloudinary tự verify bằng secret của nó. Client không bao giờ thấy secret.

**Điểm thảo luận cấp Senior:**

- Bài toán **orphan images** và tại sao nó là bài toán *eventual consistency* giữa hai hệ thống (DB & object store), giải bằng saga/cleanup job chứ không phải transaction.
- Trade-off giữa **transform-on-read** (CPU thấp, phí transform) và **transform-on-write** (kiểm soát cao, tốn CPU/lưu trữ).
- **Idempotency** trong upload (dùng content-hash làm `public_id`).

---

## 14. Best Practices — Thực hành tốt nhất

1. **Luôn lưu cặp `{url, public_id}`.** — Vì không có `public_id` thì không xoá/transform có kiểm soát được.
2. **Validate bằng magic bytes, không tin MIME header.** — Header do client kiểm soát, không đáng tin.
3. **Đặt cả `limits.fileSize` và `limits.files`.** — Chặn DoS từ file lớn *và* từ nhiều file nhỏ.
4. **Dùng `memoryStorage()` + `upload_stream`.** — Không chạm đĩa server → stateless, dễ scale.
5. **Bọc `save()` trong try/catch và `destroy()` khi lỗi.** — Giảm ảnh mồ côi ngay tại nguồn.
6. **Có cron job dọn ảnh mồ côi.** — Tuyến phòng thủ thứ hai cho cleanup phân tán.
7. **Kiểm tra ownership đúng theo luồng.** — Luồng *sửa*: `product_shop === req.user.userId` qua DB. Luồng *tạo mới (upload-first)*: sinh `productId` ở server, nhúng `shopId` (từ `req.user`) vào folder, rồi **validate prefix** `public_id` lúc tạo product. Không bao giờ tin `shopId`/`productId` từ input mà không đối chiếu.
8. **Chặn `image/svg+xml`.** — Tránh XSS qua SVG.
9. **Chỉ dùng preset transform cố định.** — Chống nổ chi phí transform.
10. **`api_secret` chỉ ở server.** — Không commit, không trả ra client, dùng signature cho (B).
11. **Map `MulterError` sang error class của dự án.** — Giữ response format nhất quán (`BadRequestError`).
12. **Upload nền qua queue khi làm hàng loạt.** — Dự án đã có RabbitMQ/Kafka; dùng cho import ảnh số lượng lớn.

---

## 15. Related Concepts — Khái niệm liên quan

Chức năng upload không đứng một mình. Nó nối với các khái niệm sau:

```
Multipart Parsing (Multer)
   └─► Object Storage (Cloudinary / S3)
          ├─► CDN & Edge Caching (giao ảnh nhanh)
          ├─► Image Transformation (resize/format on-read)
          ├─► Signed Upload (offload bandwidth)
          └─► Content Moderation (lọc nội dung)

Eventual Consistency (DB ↔ Object Store)
   └─► Orphan Cleanup Job (cron / queue)
          └─► Saga Pattern (bù trừ khi lỗi)

Security
   ├─► File Signature Validation (magic bytes)
   ├─► IDOR / Authorization (RBAC + ownership)
   └─► SSRF Prevention (upload-from-URL)

Kiến trúc dự án hiện có
   ├─► authentication (JWT) → req.user
   ├─► protect(resource) (RBAC / accesscontrol)
   ├─► OkResponse / error.response (chuẩn hoá I/O)
   ├─► winston logger + requestId (trace)
   └─► RabbitMQ / Kafka (upload nền hàng loạt)
```

**Quan hệ phụ thuộc quan trọng:**

- Upload **phụ thuộc** `authentication` + RBAC để biết *ai* upload và *có quyền không*.
- Product & User **phụ thuộc** `UploadService` — chứ không ngược lại. Đây là hướng phụ thuộc đúng: feature nghiệp vụ gọi service hạ tầng.
- Cleanup **phụ thuộc** việc đã lưu `public_id` — nếu bỏ bước này ở tầng model, toàn bộ chuỗi dọn rác sụp đổ.

---

## Phụ lục: Lộ trình triển khai đề xuất (implementation roadmap)

> Đây là thứ tự nên làm khi bắt tay code. Không phải là code — là kế hoạch.

**Giai đoạn 1 — Nền tảng (MVP, phương án A):**
1. `constants/index.ts`: `ALLOWED_MIME` (jpeg/png/webp/gif, KHÔNG svg), `MAX_FILE_SIZE = 5MB`, `MAX_FILES = 10`.
2. `validators/image.validator.ts`: `validateImageBuffer` (magic bytes).
3. `middlewares/multer.middleware.ts`: `uploadImage` + `handleMulterError`.
4. Hoàn thiện `services/index.ts`: `uploadBuffer`, `destroy`, refactor `uploadFromUrl` → trả `UploadResult`.
5. Cập nhật schema Product (`product_images`, `product_thumb_public_id` **required**) và User (`usr_avatar_public_id`); cập nhật `create.dto.ts` nhận `_id` + `product_thumb_public_id` + `product_images` (mục 4.7).
6. Config: thêm `upload.maxFileSize`, `upload.allowedMime` vào `IConfig` + `.env`.

**Giai đoạn 2 — Tích hợp Product (luồng tạo mới, upload-first):**
7. `ProductServiceFactory.prepareImages` (sinh `ObjectId` → upload song song → rollback bằng `allSettled`) — mục 5.A.
8. Route `POST /product/upload/prepare` (auth + `protect('product').create` + `multer.array` + `validateImageBuffer`).
9. Cập nhật `ProductServiceFactory.createProduct`: validate **prefix** `public_id` (mục 5.B) + dùng `payload._id` làm product `_id` trong transaction.

**Giai đoạn 2b — Tích hợp Product (luồng sửa):**
10. `ProductServiceFactory.attachImages` cho product đã tồn tại (mục 5.C) + route `PUT /product/upload/images/:productId` (`protect('product').update` + ownership qua DB).
11. Route xoá ảnh `DELETE /product/:id/images/:publicId`.

**Giai đoạn 3 — Tích hợp User avatar:**
12. `UserController.updateAvatar` + route `PATCH /user/me/avatar`.

**Giai đoạn 4 — Vận hành & bền bỉ:**
13. Cron job dọn ảnh mồ côi: liệt kê folder `products/*/*` trên Cloudinary, đối chiếu `_id` trong DB, xoá folder không có product tương ứng và cũ hơn N giờ (mục 11).
14. (Khi cần) phương án B: route `GET /upload/signature` + `POST /confirm` + verify qua Admin API.
15. (Khi cần) đẩy upload hàng loạt qua RabbitMQ/Kafka.

---

_Tài liệu này là bản thiết kế. Khi bạn sẵn sàng, mình có thể triển khai từng giai đoạn theo TDD (viết test trước) — bắt đầu từ Giai đoạn 1._
