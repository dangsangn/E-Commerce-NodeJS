[mysqld]
server_id=2
log_bin=binlog
relay_log=relay-bin
read_only=1

# Tự recover relay log nếu slave crash
relay_log_recovery=1


# Không tự start replication khi container restart
# (an toàn hơn, tự control khi nào start)
skip_replica_start=1